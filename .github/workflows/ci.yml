name: CI

on: [push, pull_request]

env:
  CTEST_OUTPUT_ON_FAILURE: ON
  CTEST_PARALLEL_LEVEL: 2

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - run: pip install gitlint pre-commit
      - run: gitlint
      - run: pre-commit run --all-files
  clang-tidy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt-get install -y clang-tidy
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DUSE_CLANG_TIDY=YES
      - run: cmake --build build --target test-include.exe test-expand.exe
  doc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt-get install -y doxygen
      - run: cmake -S . -B build
      - run: cmake --build build --target doc
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DUSE_CODE_COVERAGE=YES
      - run: cmake --build build --target check
      - uses: codecov/codecov-action@v1
  bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - run: cmake --build build --target check
      - run: cmake --build build --target bench
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DUSE_SANITIZER=YES
      - run: cmake --build build --target check
